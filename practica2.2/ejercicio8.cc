//Parametros posibles:

//-L, --dereference : sigue los links
//-f, --file-system : muestra el estado del sistema de ficheros en vez del estado del fichero
//-c --format : usa el formato especificado en vez de por defecto
//--printf : formato de imprimir por pantalla
//-t, --terse : muestra la informacion de forma abreviada
